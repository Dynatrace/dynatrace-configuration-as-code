configs:
  - id: my-bucket
    type: bucket
    config:
      template: bucket.json
      skip: true # platform only config
    environmentOverrides:
      - environment: platform_env
        override:
          skip: false

