{
    "enabled": true,
    "name": "{{ .name }}",
    "metricKey": "{{ .name }}",
    "metric": "UserActionDuration",
    "monitorIdentifier": "{{ .testId }}",
    "dimensions": [
        {
            "dimension": "Location"
        }
    ]
}
