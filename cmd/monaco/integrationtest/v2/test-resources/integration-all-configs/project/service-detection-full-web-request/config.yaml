configs:
- id: demo_rule
  config:
    name: Service detection - Full Web Request Demo Rule
    parameters:
      enabled: "false"
    template: demorule.json
    skip: false
  type:
    api: service-detection-full-web-request
