configs:
- id: globalElasticsearch
  type:
    api: extension-elasticsearch
  config:
    name: elasticsearch-ignored-name
    parameters:
      password: password
      url: https://example.com
      user: username
    template: elasticsearch-global.json
    skip: false
