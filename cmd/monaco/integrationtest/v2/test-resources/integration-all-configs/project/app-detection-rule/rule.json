{
  "applicationIdentifier": "{{ .applicationId }}",
  "name": "{{ .name }}",
  "filterConfig": {
    "pattern": "{{ .pattern }}",
    "applicationMatchType": "BEGINS_WITH",
    "applicationMatchTarget": "URL"
  }
}
