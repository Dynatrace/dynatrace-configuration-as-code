configs:
- id: myk8scluster
  type:
    api: kubernetes-credentials
  config:
    name: My Kubernetes Cluster
    parameters:
      active: "false"
      endpoint: https://mycluster/
      token: "123456"
    template: kubernetes.json
    skip: false
