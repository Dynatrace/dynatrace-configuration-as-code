configs:
- id: 5xx-errors
  config:
    name: Borg 5xx Errors
    parameters:
      tag:
        configType: auto-tag
        configId: application-tagging
        property: name
        type: reference
    template: 5xxErrors.json
    skip: false
  type:
    api: anomaly-detection-metrics
