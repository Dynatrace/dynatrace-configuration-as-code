configs:
  - id: my-bucket
    type: bucket
    config:
      template: bucket.json
      skip: false
    environmentOverrides:
      - environment: classic_env
        override:
          skip: true # platform only config

