apiVersion: v1
kind: Pod
spec:
  containers:
    - name: jnlp # nosemgrep
      image: registry.lab.dynatrace.org/keptn/ca-jenkins-agent
      securityContext:
        privileged: true
      imagePullPolicy: Always
      args:
        - "jenkins-slave"
      resources:
        requests:
          cpu: 2
          memory: 2Gi
