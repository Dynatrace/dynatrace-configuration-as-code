apiVersion: v1
kind: Pod
spec:
  containers:
    - name: monaco-build
      image: docker-proxy.lab.dynatrace.org/library/golang
      imagePullPolicy: IfNotPresent
      command:
        - sleep
      args:
        - "99d"
      tty: true
      resources:
        requests:
          cpu: "100m"
          memory: "100Mi"
    - name: monaco-runner
      image: docker-proxy.lab.dynatrace.org/library/alpine
      imagePullPolicy: IfNotPresent
      command:
        - sleep
      args:
        - "99d"
      tty: true
      resources:
        requests:
          cpu: "500m"
          memory: "4Gi"
        limits:
          cpu: "2"
          memory: "16Gi"
